package Homework;

public class Locators {

    /*
    List<WebElement> allPrices= driver.findElements(By.xpath("//*[@class='inventory_item_price']"));
     */

    /*
    List<WebElement> elements= driver.findElements(By.className("inventory_item_price"));
List<Double> prices= elements.stream().map(WebElement::getText).map(w -> w.substring(1)).map(Double::valueOf).sorted().collect(Collectors.toList());
     */

    /*
     for (WebElement tektek:allprices) {
        strinpricelist.add((tektek.getText()));
    }
    System.out.println(strinpricelist);

     */
}
